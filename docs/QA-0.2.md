# Проверки 0.2 · 12 сентября 2026

- 19 автоматических тестов пройдены: паспорта, суммы баллов, отдельность 2027, устойчивость данных, состав тренировочных ZIP и состояния таймера.
- Восемь сочетаний UI/сервера/хранилища запущены локально: проверены JSON API, состав данных V00, HTTP-ответы HTML/JS/CSS и запрет выдачи произвольного data.json. Node.js 24.19.0 и встроенный Python; минимальные заявленные версии отдельно не запускались. Результаты: `reports/stack-tests.json`.
- UI информационных систем проверен через реальный HTTP: 2026/2027, состав критериев, тема солнце/луна, сохранение темы, старт таймера, продолжение отсчёта после перезагрузки и пауза. Проверены desktop и ширина 390 пикселей; узкая таблица скорректирована для читаемости.
- PDF: тексты шести карт извлечены, суммы проверены, страницы критериев и времени отрендерены; таблица ИС 2027 визуально сверена. Все 38 исходных файлов учтены с SHA-256; внутренние документы десяти ZIP ещё не прошли полную содержательную сверку.
- Кнопка скачивания стека вызвана, ошибок JavaScript не обнаружено, однако встроенный браузер не вернул подтверждение события скачивания. Поэтому нативное сохранение кнопкой не засчитано как проверенное. Готовые ZIP дополнительно выдаются в `files/<program>/stacks/`; они сформированы тем же генератором.
- Серверная часть всех сочетаний проверена HTTP-запросами. Отдельная визуальная проверка каждого скачанного интерфейса, целевых SQL-серверов, C#/Java, нескольких вкладок таймера и часов ОС не выполнена в этом обновлении. Исторические проверки 0.1 не переносятся на новые функции.

Полные курсы и варианты 2027 не готовы; «Пробный экзамен» отключён. Публикации нет.
